# Runtime dependencies for bundled Simu JR installer.
# Keep this separate from training/test dependencies so the installer stays smaller.

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
httpx>=0.27.0

openai>=1.0.0
anthropic>=0.9.0
chromadb>=0.4.0
sentence-transformers
PyMuPDF
langchain-text-splitters
python-docx
python-dotenv
pyyaml
tqdm
reportlab

